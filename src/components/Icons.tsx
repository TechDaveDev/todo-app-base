interface Props {
  className: string;
}

export const PlusIcon = ({ className }: Props) => {
  return (
    <div>Icons</div>
  )
}

export const TrashIcon = ({ className }: Props) => {
  return (
    <div>Icons</div>
  )
}
